use std::io;
use std::io::*;
use rand::Rng;

fn main() {
    let _a: [i32; 1000000] = [0; 1000000];
    let mut _random_generator = rand::thread_rng();

    let _n;
    let _j = 0;
    let _position = 0;

    println!("Enter number of elements: ");
    
    

    
}
